/*
 * Overview
 *     Introduction
 */

console.log("\n-------------------------------------------------- 01");

{
  /*
   * RxJS 是一个响应式（reactive）编程库,
   * 通过使用 observable sequence 来构建 asynchronous 和 event-based 程序,
   * 它的核心类型是 Observable, 有六个基本的概念:
   *     Observable（可观察的）
   *     Observer（观察者）
   *     Subscription（订阅）
   *     Operator（操作符）
   *     Subject（主题）, 等价于 EventEmitter
   *     Scheduler（调度者）
   */
}
