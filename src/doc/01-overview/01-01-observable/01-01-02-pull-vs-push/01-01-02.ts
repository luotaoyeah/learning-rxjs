/*
 * Overview
 *     Observable
 *         Pull vs. Push
 */

console.log("\n-------------------------------------------------- 01");
{
  /*
   * 生产者（producer）和消费者（consumer）之间有两种不同模式的交流方式：
   *     pull
   *     push
   *
   * 所谓 pull，指的是 consumer 采取主动，当 consumer 需要获取数据时，consumer 就会主动地从 producer 中去获取数据，
   * 属于 pull 模式的有：
   *     function
   *     generator function
   *     iterator
   *
   * 所谓 push，指的是 producer 采取主动，consumer 什么时候获取数据是由 producer 决定的，consumer 只能被动地接收数据，
   * 属于 push 模式的有：
   *     promise
   *     observable
   */
}
